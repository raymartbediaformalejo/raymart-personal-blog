import SearchIcon from "./icons/SearchIcon";
import XIcon from "./icons/XIcon";

const SearchForm = () => {
  return (
    <form>
      <div>
        <SearchIcon />
        <XIcon />
        <input type="text" />
      </div>
    </form>
  );
};

export default SearchForm;
